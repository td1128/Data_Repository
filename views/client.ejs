<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Client</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        h3 {
            margin-bottom: 10px;
            font-family: Trebuchet MS, Helvetica, sans-serif;
            color:rgb(2, 13, 73);
            font-size: xx-large;
        }

        input[type="file"] {
            margin-bottom: 10px;
        }

        button[type="submit"] {
            padding: 8px 16px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button[type="submit"]:hover {
            background-color: #0056b3;
        }

        .card-img-top,
        .embed-responsive-item {
            max-width: 100%;
            height: auto;
        }
        #add {
            display: flex;
            justify-content: center;
        }
        #choose {
            display: flex;
            justify-content: center;
            align-items: center;
            border: 2px dashed #007bff; /* Border style */
            padding: 20px; /* Padding */
            border-radius: 8px; /* Border radius */
            width: 300px; /* Set width as needed */
            margin-bottom: 20px; /* Margin bottom */
        }
        h1 {
            margin-bottom: 10px;
            font-family: Trebuchet MS, Helvetica, sans-serif;
            color:rgb(2, 13, 73);
            font-size: 20;
        }
    </style>
</head>
<body>
    <form action="/addFile/<%= id %>" method="post" enctype="multipart/form-data">
        <h3>Choose files to upload</h3>
        <div id="choose">
            <input type="file" name="file" id="text_file" accept="image/*,.txt" />
        </div>
        <div id="add">
            <button type="submit" name="file" value="all">Upload</button>
        </div>
    </form>
    <hr>
    <!-- <br><br><br><br><br> -->
    <h1 class="my-6">Here's is your uploads........</h1>
    <div class="card-columns">
        <% files.forEach((file) => { %>
            <div class="card">
                <div class="card-body">
                    <% if (file.endsWith('.jpg') || file.endsWith('.png')) { %>
                        <img class="card-img-top" src="<%= `/user-${id}/` + file %>" alt="" height="250px"/>
                    <% } else { %>
                        <embed class="embed-responsive-item" type="text/html" src="<%= `/user-${id}/` + file %>" />
                    <% } %>
                    <a href="<%= `/user-${id}/` + file %>" download class="btn btn-primary">Download</a>
                </div>
            </div>
        <% }); %>
    </div>

    <!-- Bootstrap JS (optional, only needed if you want to use Bootstrap JavaScript features) -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
